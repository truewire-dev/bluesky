{
  "docs": "https://docs.bsky.app/docs/api/app-bsky-actor-search-actors",
  "meta": {
    "public": true
  },
  "spec": {
    "kind": "rpc",
    "transports": [
      "http"
    ],
    "path": "/xrpc/app.bsky.actor.searchActors",
    "method": "GET",
    "description": "Search accounts by name or handle, paged by cursor.",
    "request": {
      "title": "SearchActorsRequest",
      "type": "object",
      "description": "The query and the page.",
      "properties": {
        "q": {
          "type": "string",
          "description": "Search string, matched against handles, display names and bios."
        },
        "limit": {
          "type": "integer",
          "description": "Rows per page. Range [1, 100]; defaults to 25.",
          "minimum": 1,
          "maximum": 100,
          "default": 25
        },
        "cursor": {
          "type": "string",
          "description": "Opaque page token from the previous response; omit for the first page."
        }
      },
      "required": [
        "q"
      ]
    },
    "response": {
      "title": "ActorSearchResults",
      "type": "object",
      "description": "One page of matching profiles.",
      "properties": {
        "cursor": {
          "type": "string",
          "description": "Token for the next page; absent on the last one."
        },
        "actors": {
          "type": "array",
          "description": "Matching profiles, best first.",
          "items": {
            "$ref": "ProfileView",
            "description": "One profile."
          }
        }
      },
      "required": [
        "actors"
      ]
    }
  },
  "pagination": {
    "strategy": "token",
    "cursor": {
      "parameter": "cursor",
      "from": "cursor"
    },
    "size": {
      "parameter": "limit"
    },
    "done": {
      "kind": "absent_cursor",
      "rows": "actors"
    }
  },
  "unverified": {
    "reason": "not_captured",
    "detail": "No recording yet: the sandbox this spec was written in cannot reach bsky.app. The `record.yml` workflow replays `examples/*.request.json` through the generated client with `truewire capture` and opens a pull request with the responses."
  },
  "notes": [
    "Written from the `app.bsky`/`com.atproto` lexicons as remembered, not read: the docs host was unreachable. The schema is a subset of the lexicon; undeclared fields pass validation and are dropped from the validated value. The first recording confirms or corrects it."
  ]
}
